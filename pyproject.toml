[project]
name = "memoria-bombeo"
version = "0.1.0"
description = "Calculo de parametros para seleccion de bombas centrifugas"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0"
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]